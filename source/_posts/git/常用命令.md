# majin-git
```shell
git add . / name	# 全目录文件更新/name文件添加更新
git commit -m "msg"	#本地git分支更新
git push -u origin master	#向服务器提交分支
git pull  origin master	#下拉服务器文件
git reset --hard HEAD^	#回滚上一次提交 注意：慎用，提前备份
```

